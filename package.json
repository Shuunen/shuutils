{
  "$schema": "https://json.schemastore.org/package",
  "author": "Romain Racamier-Lafon <romain.racamier@gmail.com>",
  "bin": {
    "unique-mark": "dist/unique-mark.cjs",
    "header-injector": "dist/header-injector.js",
    "repo-lint": "dist/repo-lint.js",
    "barrel-maker": "dist/barrel-maker.js",
    "changelog-generator": "dist/changelog-generator.js"
  },
  "bugs": "https://github.com/Shuunen/shuutils/issues",
  "dependencies": {
    "tiny-glob": "^0.2.9"
  },
  "description": "utils collection",
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.14.5",
    "@types/node": "24.13.4",
    "@typescript/native-preview": "7.0.0-dev.20260707.2",
    "@vitest/coverage-v8": "5.0.0",
    "@vitest/ui": "5.0.0",
    "bun": "^1.4.2",
    "esbuild": "^0.28.2",
    "happy-dom": "^20.14.5",
    "oxfmt": "^0.68.0",
    "oxlint": "^1.83.0",
    "oxlint-tsgolint": "^7.0.2001",
    "tsup": "^8.5.1",
    "turbo": "^2.10.13",
    "typescript": "~6.0.3",
    "vite": "^8.3.0",
    "vitest": "5.0.0"
  },
  "exports": {
    ".": {
      "import": {
        "types": "./dist/shuutils.d.ts",
        "default": "./dist/shuutils.js"
      },
      "require": {
        "types": "./dist/shuutils.d.cts",
        "default": "./dist/shuutils.cjs"
      }
    }
  },
  "files": [
    "dist"
  ],
  "homepage": "https://github.com/Shuunen/shuutils#readme",
  "keywords": [
    "shuunen",
    "utils",
    "collection",
    "pure",
    "javascript",
    "typescript"
  ],
  "license": "MIT",
  "main": "./dist/shuutils.js",
  "name": "shuutils",
  "packageManager": "pnpm@12.4.2",
  "private": false,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Shuunen/shuutils.git"
  },
  "scripts": {
    "build": "turbo run build:tsc build:tsup && echo build completed successfully || (echo Please fix build errors and run pnpm run build to check for remaining build issues && exit 1)",
    "build:tsc": "tsc -b && echo build:tsc completed successfully || (echo Please fix type errors and run pnpm run build:tsc to check for remaining type issues && exit 1)",
    "build:tsup": "tsup-node && echo build:tsup completed successfully || (echo Please fix build errors and run pnpm run build:tsup to check for remaining build issues && exit 1)",
    "check": "turbo run --output-logs=errors-only lint:types lint:format lint:issues lint:repo build:tsc build:tsup test:unit && echo check completed successfully",
    "dev": "vitest",
    "lint:format": "oxfmt --check && echo lint:format completed successfully || (echo Please run pnpm run lint:format:fix to automatically fix formatting issues && exit 1)",
    "lint:format:fix": "oxfmt && echo lint:format:fix completed successfully",
    "lint:issues": "oxlint --type-aware && echo lint:issues completed successfully || (echo Please fix lint issues and run pnpm run lint:issues to check for remaining issues && exit 1)",
    "lint:repo": "bun src/bin/repo-lint.cli.ts --target=\"src/**/*.ts\" && echo lint:repo completed successfully",
    "lint:types": "tsgo -p tsconfig.app.json && tsgo -p tsconfig.node.json && echo lint:types completed successfully || (echo Please fix type errors and run pnpm run lint:types to check for remaining type issues && exit 1)",
    "list": "cd src && bun ./bin/barrel-maker.cli.ts --target=\"./lib/*.ts\" --ext=\"\" && echo list completed successfully",
    "prepack": "pnpm run build",
    "test": "turbo run test:unit",
    "test:ui": "vitest --ui",
    "test:unit": "vitest run --coverage && echo test:unit completed successfully || (echo Please fix test errors and run pnpm run test:unit to check for remaining test issues && exit 1)",
    "test:watch": "vitest",
    "up:deps": "pnpm update --latest && echo up:deps completed successfully"
  },
  "type": "module",
  "types": "./dist/shuutils.d.ts",
  "version": "11.0.0"
}
